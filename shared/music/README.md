# 共享音乐能力

该目录预留给多个音乐任务复用的稳定脚本和模板。

当前《后来没有故乡》的 API 自动化仍在任务目录内验证。只有真实跑通并经过至少一次修正后，才把稳定脚本抽到这里，避免把仍在实验的实现当成通用接口。

计划迁入：

```text
start_api_macos.sh
run_job.py
publish_review.sh
finalize_candidate.sh
templates/text2music.json
templates/cover.json
```
