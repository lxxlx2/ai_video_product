# 2026-09-12 进度记录

## 已完成

```text
Apple Silicon / arm64 预检                 PASS
ACE-Step 1.5 独立环境安装                 PASS
CPython 3.12 / uv 环境                    PASS
参考 M4A 校验与 48k WAV 转换              PASS
XL SFT 下载与 safetensors 校验            PASS
4B LM 下载与校验                          PASS
Qwen3-Embedding / VAE                     PASS
MLX DiT 加载                              PASS
MLX 5Hz LM 加载                           PASS
30 秒本地生成                             PASS
下载残片清理                              PASS
```

当前 checkpoint 正式资产约 36G：

```text
19G   acestep-v15-xl-sft
7.9G  acestep-5Hz-lm-4B
4.5G  acestep-v15-turbo
3.5G  acestep-5Hz-lm-1.7B
1.1G  Qwen3-Embedding-0.6B
337M  vae
```

## 失败实验 1：30 秒 Custom

技术状态：成功生成。

人工听感：拒绝。

关键诊断：

```text
Caption 明确写 minor key
LM 自动元数据却产生 G major
人声旋律和音准听感异常
```

结论：后续受控实验默认关闭 `thinking`、`use_cot_caption`、`use_cot_language`，除非有明确理由再开启自动规划。

## 失败实验 2：完整 5:36 Remix/Cover

技术状态：成功跑完整首。

人工听感：完全无法接受，拒绝作为候选。

结论：

1. 参考音频进入模型并不能保证高质量复刻；
2. Gradio 手工状态不适合作为长期实验记录；
3. 后续每次实验必须保存准确请求参数、API 原始响应、seed、音频 SHA-256 和试听文件；
4. 每轮尽量只改变少量参数，建立可比较 run 链。

## 目录重构

仓库开始迁移到 V0.4 分类结构：

```text
products/video/
products/music/
products/image/
products/document/
products/other/
workflows/
docs/
```

本项目新路径：

```text
products/music/later-no-hometown/
```

## 命令化工作流

已新增：

```text
workflows/music/ace-step/start_api_macos.sh
workflows/music/ace-step/stop_api_macos.sh
workflows/music/ace-step/run_and_publish.sh
workflows/music/ace-step/runner.py
workflows/music/ace-step/templates/job.example.json
```

目标链路：

```text
job JSON
-> localhost ACE-Step API
-> release_task
-> query_result
-> 下载本地原始音频
-> ffprobe
-> preview.mp3
-> manifest + log + API responses
-> git commit
-> git push
```

原始 WAV 默认留在 `.work/`，Git 每轮保存 MP3 试听版和全部轻量审计文件。

## 当前阶段工作方式

```text
ChatGPT 负责和用户讨论并修改 next-run.json
用户执行一条命令
runner 自动生成并 push
ChatGPT 从 Git 检查日志和配置
用户试听 preview.mp3
```

等 Codex Web Bridge 稳定后，Codex 直接执行同一条命令即可。

## 下一验收点

新的 REST runner 尚未在本机真实执行。

下一步只验证自动化链路：

```text
G1  Git 新分支和目录可以正常 pull
G2  start_api_macos.sh 能在 8001 启动 health API
G3  runner 可以提交一个真实音乐任务
G4  成功下载原始结果到 .work
G5  自动生成 preview.mp3
G6  runs/<run-id>/ 包含完整记录
G7  自动 commit + push 成功
G8  ChatGPT 可以从 Git 直接检查这次 run
```

全部通过以后才进入下一轮参数搜索。