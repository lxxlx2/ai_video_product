# 音乐产品

该目录用于保存歌曲、配乐、翻唱、重混、参考复现等音乐任务。

标准任务包含：歌词、风格描述、结构化 job、review/latest 审核快照、metadata、文档和最终 `output/final.wav`。

当前《后来没有故乡》验证任务仍保留在仓库根目录的 `music-later-no-hometown-local-reproduction/`，等 API 自动化验证完成并确认本地工作区可安全迁移后，再移动到 `products/music/later-no-hometown/`。